using WmMiddleware.Picking;
using WmMiddleware.Picking.Repositories;

namespace PickTestRunner
{
    class Program
    {
        static void Main(string[] args)
        {
            //var repository = new PickRepository();
            //var job = new PickJob(repository);
            //job.Execute(null);
        }
    }
}
